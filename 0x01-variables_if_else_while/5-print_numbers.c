#include <stdio.h>
/**
 * main - A program that prints a single digits of base ten
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i \n);
	}
	return (0);
}
